import { Button } from "react-bootstrap";
import { render } from "react-dom";

const FSMNode = ()=>{

        return <>
        TEST
        <Button>HELLO !</Button>
        </>
    
    
}
export default FSMNode;