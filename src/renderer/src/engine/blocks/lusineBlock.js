export class LusineBlock {

    blockName;
    code;

    /**
     * @param {string} blockName
     * @param {string} code
     */
    constructor(blockName, code) {
        this.blockName = blockName;
        this.code = code;
    }
}